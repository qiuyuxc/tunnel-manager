# Dependencies
node_modules/

# Build outputs
frontend/dist/
backend/tunnel-manager

# Local data and logs
data/
backend/data/
*.log

# Environment files
.env
.env.*
!.env.example

# Claude Code local state
.claude/

# Stray shell artifacts
-exec
-not

# Local binaries and artifacts
release/
tunnel-server
tunnel-web/
design-preview.html
*.tar.gz

# Full-coverage MiSans weights: build inputs for frontend/scripts/subset-fonts.py,
# which generates the shipped subsets into frontend/public/fonts/. Download the
# originals from Xiaomi's official MiSans page when they need regenerating.
frontend/src/assets/fonts/
backend/.accept/
backend/.fix_key.py
backend/.preview/
backend/.dedupe.py
docs/.server.py

# Local design-language references (working files, not shipped)
DESIGN.md
uDESIGN.md
