# Web UI/UX 重构 TODO

更新：2026-09-22。状态：**设计稿已通过，正式实现暂停，等待后续继续。**

## 1. 已确认的方向与边界

- [x] 阅读 `design.md`，以已完成的 App 设计语言作为 Web 视觉参考。
- [x] 制作独立交互设计稿，用户已确认「这版 UI 过，直接实现」。
- [x] 按用户最新要求暂停实现，整理后续任务；不继续更新业务页面。
- 深色石墨底、薄荷绿强调、实体内容面板、胶囊按钮与导航。
- 宽屏使用分组侧栏和工作区；手机使用「概览 / 监控 / 新建 / DNS / 更多」。
- 保留真实数据、后端 API、权限过滤、实验功能开关、表单校验和敏感操作确认。
- `design.md` 仍是原生 App 规范，不把 Web 改造进度写入原生 `design-todo.md`。
- 设计稿不是功能删减清单。正式页面中未在稿件展开的能力必须保留。

## 2. 设计稿与预览

位置：`frontend/design-preview/`。

```sh
node frontend/design-preview/serve.mjs
```

默认端口：`8083`。浏览器打开 `http://localhost:8083/`。

- 顶部选择器包含 19 个业务视图和 1 个设计说明页。
- 控制面板、隧道、监控、DNS 可切换正常、空列表、请求失败和加载状态。
- 可体验深浅主题、搜索筛选、新建弹窗、绑定确认、删除确认与移动导航。
- 示例数据只在内存中保存；不连接真实 API，刷新后重置。
- 预览服务仅提供设计稿文件，不暴露整个仓库。
- 暂停前已启动过服务；下次继续先检查 8083 是否仍在使用，不要启动重复进程。

预览曾在 Chromium 中完成 1440px、375px、320px 页面加载与截图采集，采集过程中未捕获 JavaScript 运行异常。**这不等于正式前端完成构建、视觉验收或业务回归。**

## 3. 当前代码落点：部分实现，未完成验收

以下修改已在工作区，尚未提交，不应当作完成版发布：

| 文件 | 已落盘的修改 | 仍需完成 |
| --- | --- | --- |
| `frontend/src/design-tokens.ts` | 新增深浅色与兼容暖色调色板，生成 CSS 语义变量 | 核对色彩语义、对比度、主题覆盖优先级 |
| `frontend/src/theme.ts` | Naive UI 主题改为使用共同调色板 | 检查安装版本的主题字段类型和控件实际外观 |
| `frontend/src/main.ts` | 挂载设计令牌样式 | 验证首屏及公共页面主题表现 |
| `frontend/src/styles.css` | 移除旧重复配色块，调整基础间距与圆角令牌 | 按设计稿完成公共按钮、表单、卡片、表格和响应式样式 |
| `frontend/src/stores/config.ts` | 未保存偏好时默认深色，增加轻量效果偏好 | 接入设置入口和 CSS，验证原有偏好与表单状态不丢失 |
| `frontend/index.html` | 首屏恢复深浅主题与轻量效果偏好 | 验证无主题闪烁，已有浅色偏好不被覆盖 |

导航重构补丁执行失败，**尚未落盘**：`NavBar.vue`、`App.vue`、`MobileNav.vue`、`navigation.ts` 仍是原实现；拟新增的 `BrandMark.vue`、`ConsoleHeader.vue` 尚不存在。后续应重新实现，不要误认为已经完成。

正式业务页面尚未改造。曾启动类型检查，但中断后无法取回该进程结果；**正式前端类型检查、构建和测试均未确认通过**。

## 4. 后续实施清单

### A. 恢复上下文与公共主题

- [ ] 先看 `git status`、`git diff` 和本文件，区分本轮遗留修改与后续用户修改。
- [ ] 检查 `frontend/src/design-tokens.ts` 与 `theme.ts` 的一致性；修正类型不兼容项。
- [ ] 完成公共样式：22px 面板、14px 输入、胶囊按钮、单一主要动作、红色危险操作。
- [ ] 标题、正文、辅助文案分层；数值用等宽数字，域名、ID 和令牌可完整查看。
- [ ] 控制玻璃范围，仅用于导航与轻浮层；正文、表格、图表保持实体背景。
- [ ] 深浅主题切换不重建页面、不清空表单；保留已有用户偏好及暖色主题兼容性。
- [ ] 接入轻量效果开关，并遵循 `prefers-reduced-motion`。

### B. 导航与公共框架

- [ ] `navigation.ts`：分为工作空间、网络、管理、个人，继续使用原权限过滤。
- [ ] `NavBar.vue`：按稿件重构品牌、当前账户/实例信息、分组导航、选中胶囊；保留折叠与退出登录。
- [ ] 新增公共品牌组件，默认使用隧道图形，同时保留站点自定义图标。
- [ ] 新增桌面顶部栏：当前位置、主题切换、通知和账户入口。
- [ ] 快速前往使用现有允许访问的导航项，支持搜索、键盘快捷键和关闭；不新增后端能力。
- [ ] `MobileNav.vue`：浮动胶囊底栏、新建与更多弹层；分组更新后不可漏掉隧道入口。
- [ ] `SheetPanel.vue`：统一弹层圆角、关闭触控区域、焦点、Escape、遮罩和底部安全区。
- [ ] `App.vue`：接入框架，预留移动导航空间；登录、安装、访客首页和公开状态页保持独立布局。

### C. 业务页面落地

| 任务 | 正式文件 | 必须保留 |
| --- | --- | --- |
| [ ] 控制面板 | `frontend/src/views/Dashboard.vue` | 真实配置与监控概览；平均、峰值、异常、不​​可达及明细语义，不能换成示例图表 |
| [ ] 隧道管理 | `frontend/src/views/Tunnels.vue` | 查询、创建、选择、删除确认、连接令牌与安装说明 |
| [ ] 隧道详情 | `frontend/src/views/TunnelDetail.vue` | 完整连接信息、路由、敏感凭据及明确返回路径 |
| [ ] 服务监控 | `frontend/src/views/Monitors.vue` | 真实项目状态、创建与详情入口、错误和空状态 |
| [ ] 监控详情 | `frontend/src/views/MonitorDetail.vue` | 服务与探测配置、记录、图表、公开页面设置及事件明细 |
| [ ] 域名绑定 | `frontend/src/views/DomainBinding.vue` | 简单/优选模式、源站编辑、CNAME、辅助回源、校验和提交反馈 |
| [ ] 批量绑定 | `frontend/src/views/BatchDomainBinding.vue` | 原有批量输入、校验、处理过程和逐项结果 |
| [ ] DNS 管理 | `frontend/src/views/DNSManagement.vue` | 区域选择、搜索、记录类型校验、代理、TTL、批量修改与删除 |
| [ ] IP 实验室 | `frontend/src/views/LabIPSelector.vue` | 管理员与实验开关限制、真实探测、自动化、华为云 DNS 和历史 |
| [ ] 全局设置 | `frontend/src/views/Settings.vue` | 站点品牌、默认 CNAME、回退源与真实配置项；补齐外观入口 |
| [ ] TG 机器人 | `frontend/src/views/TelegramSettings.vue` | 凭据、运行状态、授权会话与真实测试反馈 |
| [ ] 通知设置 | `frontend/src/views/Notifications.vue` | 现有渠道、触发事件、配置校验与发送测试 |
| [ ] 管理后台 | `frontend/src/views/Admin.vue` | 用户、用户组、注册、安全、权限及审计，不能只剩稿件的简版表格 |
| [ ] 账户设置 | `frontend/src/views/Account.vue` | 身份、邮箱、密码、通行密钥、双重验证、Cloudflare 授权和会话 |
| [ ] 关于与更新 | `frontend/src/views/About.vue` | 真实版本与更新检查，不使用设计稿固定值代替 |
| [ ] 登录 | `frontend/src/views/Login.vue` | 登录、注册、找回密码、通行密钥与多因素验证等现有流程 |
| [ ] 首次安装 | `frontend/src/views/Setup.vue` | 数据库选择、连接配置、管理员初始化和安装状态判断 |
| [ ] 访客首页 | `frontend/src/views/Landing.vue` | 站点定制、登录状态、公开入口与首页开关 |
| [ ] 公开状态页 | `frontend/src/views/PublicStatus.vue` | 真实共享配置、主题、标题、图标、状态刷新及隐私边界 |

### D. 验证与交付

- [ ] 在 `frontend/` 运行 `npm test`。
- [ ] 在 `frontend/` 运行 `npm run build`，确认 `vue-tsc` 和 Vite 均通过。
- [ ] 浏览器检查 1440px / 1280px、375px、320px，覆盖深浅主题和长中文/域名。
- [ ] 检查表格滚动、表单错误、空数据、加载、请求失败、会话过期和权限不足。
- [ ] 键盘可到达并操作导航、分段控件和弹层；关闭后恢复焦点；底栏不遮挡最后一个按钮。
- [ ] 核对主题切换不丢输入或滚动位置，公开状态页的主题不会污染其他页面。
- [ ] 对照批准设计稿核验结构，不把「全站换色」当成「页面改造完成」。
- [ ] 验证使用授权测试环境或浏览器端模拟响应，不对真实服务执行创建、删除、发信或探测操作。
- [ ] 所有检查通过后，再将 8083 从独立设计稿切换为正式前端预览；先确认后端地址和代理。
- [ ] 完成后处理独立设计稿：保留必要设计依据，归档或移除临时原型，不将示例逻辑接入正式路由。
- [ ] 未经另行要求，不自动提交、推送或发布。

## 5. 下次继续的起点

从 **A：检查已落盘的主题修改并完成公共样式** 开始，然后实施导航和页面。无需重新询问设计方向，方案已经通过；当前暂停仅意味着暂不继续实施，不代表未完成修改已通过验收。
